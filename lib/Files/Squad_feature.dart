import 'package:flutter/material.dart';

class Squad_feature extends StatelessWidget{
  const Squad_feature({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.red,
        child: Text('Squad screen'),
      ),
    );
  }
}
import 'package:flutter/material.dart';

class Me_feature extends StatelessWidget{
  const Me_feature({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.red,
        child: Text('Me screen'),
      ),
    );
  }
}
import 'package:flutter/material.dart';

class Football_feature extends StatelessWidget{
  const Football_feature({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        color: Colors.red,
        child: Text('Football screen'),
      ),
    );
  }
}